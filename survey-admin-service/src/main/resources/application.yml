spring:
  application:
    name: survey-admin-service
  profiles:
    active: local
  config:
    import: optional:configserver:http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: "*"